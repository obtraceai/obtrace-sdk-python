# Configuration

Required:
- `api_key`
- `ingest_base_url`
- `service_name`

Optional:
- `service_version`
- `tenant_id`, `project_id`, `app_id`, `env`
- `request_timeout_sec`, `max_queue_size`, `debug`
